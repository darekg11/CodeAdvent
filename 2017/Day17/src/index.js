const circleBufferAlgo = require("./circleBufferAlgo");

const result = circleBufferAlgo.startBuffering(355);

const valueAfterZero = circleBufferAlgo.findValueAfterZero(355);

console.log(result);

console.log(valueAfterZero);
